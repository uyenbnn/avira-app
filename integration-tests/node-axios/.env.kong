# Route all service calls through Kong API Gateway
AUTH_BASE_URL=http://localhost:8000/api
PROJECT_BASE_URL=http://localhost:8000/api
INIT_BASE_URL=http://localhost:8000/api
USER_BASE_URL=http://localhost:8000/api
IAM_BASE_URL=http://localhost:8000

# Registration + login credentials (a unique username is generated per run)
TEST_USER_PASSWORD=Test123!

# Optional admin checks
ADMIN_EMAIL=admin@avira.local
ADMIN_PASSWORD=admin123

# Set true to bootstrap keycloak + messaging before tests
RUN_INIT=false


